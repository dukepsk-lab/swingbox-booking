# คู่มือตั้งค่าเมนู (Google Sheet → CSV)

ระบบเมนูของ `menu.html` ดึงข้อมูลจาก **Google Sheet** ที่ publish เป็น CSV
แก้เมนูได้โดย**แก้ในชีทอย่างเดียว** ไม่ต้องแก้โค้ดหรือ deploy อะไรเพิ่ม

> ตั้งค่าครั้งเดียวจบ หลังจากนั้นแค่พิมพ์ในชีท → หน้าเว็บอัปเดตเอง

---

## ขั้นตอนตั้งค่า (ทำครั้งเดียว)

### 1. สร้าง Google Sheet ใหม่
- ไปที่ https://sheets.google.com แล้วสร้างชีทใหม่ ตั้งชื่อเช่น **SwingBox Menu**
- เป็นคนละไฟล์กับชีท booking ได้เลย (แยกกัน)

### 2. นำเข้าข้อมูลตั้งต้น
- เมนู ▸ **File ▸ Import ▸ Upload** แล้วเลือกไฟล์ `menu-data.csv` (อยู่ในโปรเจกต์นี้)
- เลือก **Replace current sheet** แล้ว Import
- จะได้ตารางพร้อมเมนูปัจจุบันครบทุกคอลัมน์

### 3. Publish เป็น CSV
- เมนู ▸ **File ▸ Share ▸ Publish to web**
- แท็บ **Link**:
  - ช่องซ้าย: เลือก **แท็บ (sheet)** ของเมนู (ไม่ใช่ Entire Document)
  - ช่องขวา: เลือก **Comma-separated values (.csv)**
- กด **Publish** แล้วยืนยัน
- **คัดลอกลิงก์** ที่ได้ (จะหน้าตาประมาณ
  `https://docs.google.com/spreadsheets/d/e/XXXX/pub?gid=0&single=true&output=csv`)

### 4. ใส่ลิงก์ลงในโค้ด
- เปิดไฟล์ `menu.html` หาบรรทัด:
  ```js
  const MENU_CSV_URL = ""; // <<< วางลิงก์ CSV ที่ publish ตรงนี้
  ```
- วางลิงก์จากขั้นที่ 3 ลงในเครื่องหมายคำพูด เช่น:
  ```js
  const MENU_CSV_URL = "https://docs.google.com/spreadsheets/d/e/XXXX/pub?gid=0&single=true&output=csv";
  ```
- commit + push ไฟล์ `menu.html` ครั้งเดียว — เสร็จ

> ถ้ายังไม่ได้ใส่ลิงก์ หน้าเมนูจะแสดง **เมนูสำรอง** (ชุดปัจจุบัน) ไว้ก่อน ไม่หน้าเปล่า

---

## วิธีแก้เมนู (หลังตั้งค่าแล้ว)

ทำทุกอย่างใน Google Sheet ได้เลย:

| อยากทำ | ทำยังไง |
|---|---|
| **เพิ่มเมนู** | เพิ่มแถวใหม่ กรอกข้อมูลตามคอลัมน์ |
| **แก้ราคา/ชื่อ** | แก้ในเซลล์ |
| **ลบเมนู** | ลบทั้งแถว |
| **ทำเครื่องหมาย "หมด"** (ขีดฆ่า) | ใส่ `TRUE` ในคอลัมน์ `soldout` |
| **ติดดาว ★ (Signature)** | ใส่ `TRUE` ในคอลัมน์ `signature` |
| **จัดลำดับ** | แก้ตัวเลขคอลัมน์ `order` |

ผลจะขึ้นบนเว็บภายในไม่กี่นาที (Google cache ตัว CSV ไว้สั้นๆ) — รีเฟรชหน้าเว็บได้

---

## คำอธิบายคอลัมน์

| คอลัมน์ | จำเป็น | ความหมาย |
|---|---|---|
| `order` | ไม่ | ลำดับการเรียง (ตัวเลขน้อยขึ้นก่อน) เว้นว่างได้ |
| `section` | ใช่ | กล่องใหญ่ — ใช้ **key**: `non_alcohol` หรือ `alcohol` |
| `category` | ใช่ | หมวดย่อย — ใช้ **key**: `soft_drinks`, `mixers`, `snacks`, `beer`, `spirits`, `wine` |
| `name_en` `name_th` `name_jp` `name_cn` | อย่างน้อย 1 | ชื่อเมนูแต่ละภาษา (ช่องไหนว่าง จะใช้ `name_en` แทน) |
| `desc_en` `desc_th` `desc_jp` `desc_cn` | ไม่ | คำอธิบาย/หมายเหตุใต้ชื่อ |
| `price` | ใช่ | ราคา ใส่แค่ตัวเลข เช่น `59` (ระบบเติม ฿ ให้) หลักพันใส่ `"1,390"` |
| `signature` | ไม่ | `TRUE` = ติดดาว ★ |
| `soldout` | ไม่ | `TRUE` = ขีดฆ่า (หมด) |

> ค่าที่นับเป็น "จริง": `TRUE`, `1`, `x`, `yes` (ปล่อยว่าง = ไม่ใช่)

---

## เรื่องภาษา (TH / EN / JP / CN)

- **ชื่อเมนู / คำอธิบาย**: แก้ในชีทได้เลยตามคอลัมน์ `_th` `_en` `_jp` `_cn`
  ช่องไหนเว้นว่างจะแสดงภาษาอังกฤษ (`name_en`) แทนอัตโนมัติ
- **หัวข้อหมวด** (เช่น "เบียร์ / Beer"): แปลไว้ในไฟล์ `menu.html` ตัวแปร `HEADINGS_I18N` แล้ว
  ถ้าจะ **เพิ่มหมวดใหม่** ให้เพิ่ม key + คำแปลในนั้น เช่น:
  ```js
  cocktail: { th:'ค็อกเทล', en:'Cocktail', jp:'カクテル', cn:'鸡尾酒' },
  ```
  แล้วใช้ key `cocktail` ในคอลัมน์ `category` ของชีท
  (ถ้าไม่เพิ่มคำแปล หมวดใหม่จะแสดงชื่อ key ดิบไปก่อน)

---

## ทดสอบ
รันเซิร์ฟเวอร์ static แล้วเปิดหน้าเมนู:
```bash
python3 -m http.server 8000
# เปิด http://localhost:8000/menu.html
```
- ลองสลับภาษาบนแถบเมนู → ชื่อ/หมวด/คำอธิบายต้องเปลี่ยนตาม
- ลองแก้ชีทแล้วรีเฟรช → เมนูบนเว็บเปลี่ยนตาม
